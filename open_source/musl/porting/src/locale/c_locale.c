#include "locale_impl.h"
#include <stdint.h>

/* Locals in shrot are collections of languge and country specific conventiions allowing to adapt software to user's
 * preference */

const struct __locale_struct __c_locale = { };
const struct __locale_struct __c_dot_utf8_locale = {};
